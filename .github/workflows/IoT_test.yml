name: C/C++ CI

on:
  push:
    branches:
      - IoT_GoogleTestingVs
  pull_request:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install CMake
        run: sudo apt-get install -y cmake

      - name: Build
        run: |
          mkdir build
          cd IoT/SEP4/Test
          cmake ..
          cmake --build .

      - name: Test
        run: |
          cd IoT/SEP4/Test
          ctest --verbose
